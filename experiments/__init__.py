"""Proof fixtures and drafts that are NOT games a person plays.

`games/<name>/` is a rung: it earns its slot by exposing one named executable
risk to information isolation or referee judgment that no other rung expresses,
and `core/registry.py` lists it so a person can sit down at it. Nothing here
does. These are kept because they prove something or are still being drafted,
and `AGENTS.md` says a rung in `games/` is neither of those.
"""
