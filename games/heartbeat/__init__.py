"""A spike, not a game. See ``heartbeat.py``.

An off-map faction with no seat, driven by a counted tick clock derived from the
seed, inside a loop. It exists to answer one question - whether a render can be
audited against the entitlement snapshot that held when it was BUILT - and it is
not registered, has no console seat, and gates nothing.
"""
