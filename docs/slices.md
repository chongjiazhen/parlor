# Closed session slices

The finished half of `queue.md`'s slice table, moved out 2026-08-28 so the queue
file carries live rows only. **Rows are struck and annotated, never deleted** -
live rows cite slices by name (S6's pre-committed criterion rests on "the baseline
derived by S3"), and the numbers are IDs, not positions. Do not renumber them.

Verbatim from `queue.md`; the annotations are the ones written when each slice
landed. `queue.md` keeps the open rows.

| # | slice | needs | entry condition | done when |
|---|---|---|---|---|
| ~~**S1**~~ | ~~Call cabal gate #3.~~ **CALLED 2026-08-27** - 3a abandoned at every table size, 3b gets one pre-committed 40-game campaign. Verdicts: `docs/gate3a-retired.md`, `docs/gate3b-verdict.md`. | - | - | done |
| ~~**S9**~~ | ~~Code debt, commit one - the integrity surface.~~ **LANDED 2026-08-28** - items 7, 1, 2 and 4 in one commit. `over_sabotage`'s docstring now states the benchmark as "the pair failed to find a convention"; the integrity block moved to `core/integrity.py`, shared by both games, and gained a witnessed rate per seat-game, a `recovered` third outcome, and a clean-game count. `fallback_rate` is unchanged and keeps its name - both reproducers still agree with the recorded runs. Seven guards mutation-checked, each killed by its own named test. | - | - | done |
| ~~**S10**~~ | ~~Code debt, commit two - changeling's knowledge class.~~ **LANDED 2026-08-28** - the class is keyed on what the seat was TOLD; a MEET card that met nobody is `none`, not `identity`. The pin was replaced, not deleted: `..._never_the_DAWN_card` keeps the half that still holds, beside `..._keyed_on_what_the_seat_was_TOLD` and the property itself. Four guards mutation-checked. Stratum sizes re-measured and now recomputable - `py -3 -m eval.strata` - which moves 2375 of 20000 seat-nights from `identity` to `none` and recovers ~19% of the blind stratum. Both expansion decks are unblocked. | - | - | done |
| ~~**S2**~~ | ~~changeling: clean re-run, then 200 games.~~ **BOTH HALVES LANDED 2026-08-28.** `PARLOR DONE rc=0 games=200/200 elapsed=18250s`, `--arm llm`, seed 4000, `qwen36-35b-a3b-iq3` at 100% attribution, 0.40% fallback. Records `eval/records/s2.json` + `s2.json.jsonl`. The powers half ran the same day: 2x20 on the fixed lane, identical deals, `PARLOR PAIR DONE rc=0 arms=2/2`, recipe `eval/runs/changeling-powers-pair.cmd`. The `RULES.md` powers table is on clean numbers and its caveat block is gone; the accuracy gain is banked. The two rule-error counts were the half it could not close; they were re-measured 2026-08-28 with a tracked instrument (`eval/rule_errors.py`) and the fall turned out not to be established. | - | - | done |
| ~~**S3**~~ | ~~cabal scorer honesty.~~ **LANDED 2026-08-27** - all four derived numbers now come from the knowledge model or the record. The bar for S6 is unchanged (`SETUP_5` legal set is 3, so the derived chance IS 1/3); the audit's role-outing count was near-zero by construction and is not. See the measured rows below. | - | - | done |
| ~~**S4**~~ | ~~Ops hygiene.~~ **LANDED 2026-08-27** - `core/runlog.py` writes `PARLOR DONE rc=N games=L/R elapsed=Ns` from both eval drivers; both games record a fallback's REASON per decision on a `refused` field; the untracked `run-hunt20.cmd` is retired and its exact invocation preserved in `eval/runs/hunt-local.cmd`. Records changed, play did not - the bytes a model receives are identical, so S6 may freeze on this code. | - | - | done |
| ~~**S5**~~ | ~~changeling: read the 200-game run.~~ **PRE-CHANGE READ 2026-08-28 - GATE #3 HOLDS.** Blind villager accuracy **110/247 = 44.53%**, Wilson floor **38.47%** and the scorer's game-bootstrap floor **37.36%**, both clearing the pre-committed 35.95%. Gate #2 readable at **59.49%** [52.48%, 66.13%] and given **no verdict**, per the criterion. Two clauses did not apply cleanly and are recorded rather than smoothed: the criterion said Wilson where the scorer publishes a bootstrap, and S2 ran no random arm so the own-arm clause had nothing to fire on. Writeup `games/changeling/RULES.md` §S2 read; arithmetic `py -3 -m eval.s5_verdict`. Code-debt item 4 settled in the same session because the writeup had to quote a path. **S14 changed a model-facing self-line, so this result is not a verdict on post-S14 wording.** | - | - | done |
| ~~**S6**~~ | ~~The gate #3b campaign - cabal's LAST GPU program.~~ **CALLED 2026-08-27 - gate #3b NOT SHOWN, cabal's GPU program stops.** 40/40 games, both arms `rc=0`, 1.35% campaign fallback. Pooled 9/20 = 45.00%, Wilson [25.82%, 65.79%] against the derived bar 33.33% - floor does not clear, so the pre-committed answer applies and there is no third campaign. All three draw-dependent items resolved off the same records: step-not-slope did NOT fire, the `five_rejects` shift is not established, run-length degradation did not reproduce. Verdict and arithmetic: `docs/gate3b-verdict.md`, `py -3 -m eval.s6_verdict`. | - | - | done |
| ~~**S7**~~ | ~~Measured prompt variables.~~ **DROPPED as a cabal GPU program** - a paired cabal arm is 13.2h to move a number 3a no longer spends precision on. Re-homed; reasoning in `docs/gate3a-retired.md`. | - | - | done |
| ~~**S11**~~ | ~~The DURF session engine.~~ **LANDED 2026-08-28 - gate #1 measured on this rung for the first time, and it HELD IN 3 OF 6 SESSIONS.** The engine, the fact-keyed entitlement, the audit and the transcript renderer are in `games/durf/` (`facts.py`, `kernel.py`, `session.py`, `seats.py`, `transcript.py`), driver `eval/durf_session.py`, recipe `eval/runs/durf-session.cmd`, fact set `games/durf/fixtures/facts.json`. `core/` was not touched and `find_leaks` is unchanged - the rung's adapter NUMBERS its facts and hands the matching to the primitive, so there is one naive matcher in the repo and not two. 86 tests, seven guards mutation-checked. **The read, the three leaks and the one term that is on the record as arguable live in `docs/durf-rung.md` §The session engine - do not restate them here.** Evidence: `transcripts/durf-session-q36-{leak,held}.md`. | - | - | done |
| ~~**S12**~~ | ~~The delegation lane.~~ **CLOSED 2026-08-30** - seven graded dispatches, 3 pass / 4 fail, one landed untouched. `docs/worklane.md` is the contract and carries what the batch returned; `.scratch/lane/README.md` is the index of what is still dispatchable and to whom. | - | - | done |
| ~~**S8**~~ | ~~Run belfry model-adjudicator arms.~~ **READ 2026-08-31.** S8 voided at 12/20 (60.00%) adjudicator fallback because the served model wrapped valid JSON in a Markdown fence; evidence: `transcripts/belfry-adjudicator-s8-void.md`. S8b bound a narrow parser normalization, fresh criterion and fresh pair. Both player fallback rates were 0/2809 and model adjudicator fallback 0/20. Held-out source accuracy was **88.89%**, above Wilson 95% chance upper endpoint **70.97%**: **DISTINGUISHABLE**. This is a bounded setup-choice trace result, not choice quality or general referee quality. Evidence: `transcripts/belfry-adjudicator-s8b.md`; arithmetic: `py -3 -m eval.belfry_adjudicator_verdict eval/records/belfry-adjudicator-v2-control.json eval/records/belfry-adjudicator-v2-model.json --v2`. | - | - | done |
| ~~**S14**~~ | ~~Changeling deal-versus-dawn wording.~~ **CLOSED 2026-08-31T13:39:45.9438602Z** - `self_line` now names the card dealt, while the entitled night reveal carries any later self-belief. Regression covers a seat whose post-night belief differs from its deal; gate #1 audit guards remain green. S5 is marked pre-change because this changes bytes sent to its model. | - | - | done |
| ~~**S15**~~ | ~~Cabal solver-seat arm.~~ **CLOSED 2026-08-31T16:30:30.3689832Z** - `SolverPolicy` reads only `evidence_from_referee` at VOTE and acts only on mechanically certain clean/tainted teams; mixed evidence and every other phase stay on `RandomPolicy`. `py -3 -m games.cabal.demo --solver --seed 3` completed gate #1 clean with 0 fallbacks. 112 focused cabal tests passed (5 skipped). No measurement claimed. | - | - | done |
| ~~**S13**~~ | ~~Claim-shaped self-outing re-score.~~ **CLOSED 2026-09-01T00:31:53.6091592Z** - `outed_own_role_in_public` now counts explicit first-person present-tense claims rather than every own-role mention. Re-score: `hunt20b` 0/1150 at 0.54% fallback; `hunt20c` 7/1580 at 1.78% fallback. The 26/1580 theme-name read was ordinary role talk, not self-outings. 18 audit tests pass. | - | - | done |
| ~~**S35**~~ | ~~Per-call usage instrumentation.~~ **CLOSED 2026-09-01T08:43:43Z** - the fields landed 2026-08-30 in `f9d7ae2` copied into all four games; they are now DECLARED ONCE as `core/callcost.CallCost` and inherited, with `forget`/`note`/`spent` replacing the six lines each game repeated. `kw_only` is what makes one declaration possible: a defaulted base field would otherwise sit in front of every game's required `turn`. Values and payload are unchanged - only the JSON key ORDER moves, the three cost keys now leading each decision object. Old records still load (`core/test_callcost.py`, legacy key sets pinned from `eval/records/s2.json.jsonl`); `belfry-live1` carries usage on all 5515 decisions, e.g. `{'prompt_tokens': 809, 'completion_tokens': 301}`. 864 tests pass, four demos smoke clean, five guards mutation-checked each by its own named test. The worker draft is superseded and its branch swept; tag `rejected-slotB-20260830` keeps it. | - | - | done |
| ~~**S32**~~ | ~~`--v2` default record paths.~~ **CLOSED 2026-09-01T10:00:16.3235876Z** - the arm binding is one `ARMS` object per criterion: record paths, expected settings and criterion document move together or not at all, the shape `belfry_live1_verdict` already uses. `--v2` survives as an alias because `eval/runs/belfry-adjudicator-v2.cmd` is a tracked recipe and `docs/slices.md` cites that spelling as S8b's arithmetic. Six tests on the binding, two of which fail against the old default (mutation-checked). The cited S8b command reproduces 88.89% over a 70.97% chance ceiling unchanged. | - | - | done |
| ~~**S34**~~ | ~~Referee transcript handle.~~ **CLOSED 2026-09-01T14:02:31+00:00** - `ChangelingReferee` is a context manager, so the sidecar closes on every exit path including a raising one. It was closed on each caller's LAST line, which only runs when that line is reached: an assertion failing above it left the handle open, `TemporaryDirectory` cleanup raised `PermissionError [WinError 32]`, and pytest reported THAT instead of the real failure - masking every other failure in the block too. `demo.py`'s try/finally already held; `close()` stays idempotent so the two compose. Mutation-checked: `__exit__` neutered to `_ = exc` reproduces the WinError 32 mask. 131 -> 133 collected. Merged `4c917e0`. | mechanical | inline | done |
| ~~**S30**~~ | ~~`cloud_strata` random rows.~~ **CLOSED 2026-09-01T14:02:31+00:00** - mixed-arm records score; a seat no upstream served lands in its own cell, never pooled into `UNATTRIBUTED` (which means a model served the row and provenance was erased - a different denominator), and the `ValueError` survives for a row that is neither. The cell is **NON-MODEL, not RANDOM**: `HeuristicPolicy` and `SolverPolicy` set no `last_upstream` either, so a RANDOM label would report the solver arm (live since `3e92509`) as random play. Safe only because `Backend.complete_meta` returns `str(body.get("model") or self.model)` and never empty - that invariant is stated where the classifier reads it and pinned by a test. Worker diff kept, its docstring reasoning and cell name replaced. 8 -> 13 collected. Merged `0ed697b`. | constrained | opencode + inline review | done |
| ~~**S36**~~ | ~~A lane slot runs a suite with a hole in it.~~ **CLOSED 2026-09-01T14:38:47+00:00** - absence of a record is now a SKIP with a reason, not a collection error, and `eval/test_deduction.py` collects everywhere. Neither candidate in the row was taken: no junction, and 4.5 MB of run output stays untracked. The line is drawn on the DIRECTORY (`eval/records_gate.py`) - empty means a slot or a clone and skips; populated-but-missing-this-file means an instrument is citing a record that has been renamed or deleted, and FAILS. **The second half was the reason the chain could not see it:** `scripts/testfloor.py` discarded pytest's return code, and pytest prints the ids it reached before interrupting, so a module that never entered the suite read as a merely smaller one. It now returns -1 on a non-zero collect and prints the output. Two synthetic `TestControl` mutation guards moved to a class that needs no records, so a slot still runs them. Slot: 5 errors -> 0, eval 394 -> 404 collected, identical in both trees. Both new guards mutation-checked - and the first mutant SKIPPED through `assertRaises(AssertionError)` rather than failing, which is why `assertRotDetected` catches `SkipTest` by hand. | constrained | inline | done |
| ~~**S23**~~ | ~~Belfry discretion-quality criterion.~~ **CLOSED 2026-09-01T16:05:53Z** - the criterion is `docs/belfry-discretion-quality-criterion.md`, scorer `eval/belfry_steering_verdict.py`, recipe `eval/runs/belfry-steering.cmd`. Nothing launched, which is the whole done-when. **The row asked for a quality question and the board does not have one:** `grim.herring` has exactly one reader (`Grimoire.registers_demon`, applied only when an asker is named) and exactly one caller through it (`night.divine`), and compact five seats deal no outsiders, so the menu is the three townsfolk and they are mechanically exchangeable. A rubric over them would re-report S8b's seat-index prior, and a taste ground truth nobody here holds is the general-referee claim the row forbade. What is graded instead is STEERABILITY - board plus one stated placement rule in the ask, compliance against chance 1/3 - with the seeded-random arm's own compliance as the chance-neutrality control and the menu offered in a seeded order so a position prior cannot score against a rule it never read. `--adjudicator-steer` is off by default, so every existing arm is byte-identical; S8b re-reads at 88.89% unchanged. Mutation-checked: sorting the offered menu turns the position-prior fixture from NOT SHOWN into STEERED. **RUN THE SAME DAY** (the row asked for no launch; the model was armed and the arm is cheap): 360 paired games, 0/16568 player fallback both arms, 0/152 steered adjudicator fallback, compliance **46.05%** over a **40.71%** chance ceiling with the random control at 32.24% inside its own interval - **STEERED**, and the rule ignored on 82 of 152 calls. `docs/measurements.md` and `transcripts/belfry-steering-s23.md`. | judgment | inline | done |
| ~~**S31**~~ | ~~`audit_decisions` ascii dispatch.~~ **CLOSED 2026-09-01T14:49:03+00:00**, both halves. `_says` word-boundaries a Latin name and contains a CJK one, so `1984-cn` is checked at all. `_claims_own_role` returns `claim` / `mention` / `""` and routes on the skin's DECLARED language - `Theme.lang`, new field, `zh` on the two cn skins and `en` everywhere else. **The floor, which was the design call:** a language with no rule in `CLAIM_RULES` falls back to plain mention rather than to zero, so the count for such a skin is an UPPER bound and the first note line says so where `--show` cannot truncate it. Over-counting loudly is the trade; a 0 in that column is indistinguishable from a table that never outed itself, which is the 0/1290 bug one skin later. **Language is declared, never guessed from the name or the script** - a Japanese skin names roles in the same kanji as a Chinese one and says none of 我是, and a French one is Latin like English; that mislabelling is held as a fixture and its silent 0/1 is pinned. Five new tests on two fake kanji skins, four mutants killed each by its own named test. S13's figures reproduce unchanged: `hunt20b` 0/1150, `hunt20c` 7/1580. | judgment | inline | done |
| ~~**S33**~~ | ~~Changeling self-leak rename.~~ **CLOSED 2026-09-02 - the premise was wrong and the real defect was next door.** `dealt.key != held.key` is not a bypass: where the dealt and dawn cards are the same card, an honest renderer and `LeaksOwnTruth` emit byte-identical lines - 129 of 15000 seat-games, identical in all 129 - so nothing distinguishes them and there is no leak to catch. No rename, no model-facing change, **S5 not re-baselined and S18 unblocked**. What the read did find is a false negative in the OTHER scan: it excluded the viewer, while `TAKE` and `WAKE` record a `Knowledge` about the acting seat itself, so a referee refreshing that stale self-reveal to dawn truth leaked in the third person with nothing looking - **470 leaks over 15000 seat-games, none caught**. The viewer is an ordinary seat in that scan now, `entitled_seats` holds the entitlement, 0 false positives over the same 15000. Three tests added, the catching one mutation-checked by restoring the exclusion. `games/changeling/RULES.md` §gate #1. | - | - | done |
| ~~**S16**~~ | ~~False-claim instrument.~~ **CLOSED 2026-09-01T14:57:26+00:00**, inline rather than by the `codex` the row named - it is S13's claim shape pointed at the other role, so it inherits `CLAIM_RULES` and there is one claim matcher in the repo, not three. `claimed_a_role_it_was_not_dealt` counts a seat claiming a role it does NOT hold, disjoint from S13's self-outing by construction and pinned that way. **The floor convention is INVERTED here, and that is the design call:** a language with no rule gets S31's mention floor in the self-outing check, but naming a role you do not hold is the most ordinary sentence at the table, so a containment match would flag most of the record - those games are REFUSED out of the denominator instead, named on the first note line. **`--control` is the other half:** re-score with every deal moved one seat along, speech untouched, and the two counts must trade places; both zero means the matcher never fired and the tool exits 3 rather than publishing it. It immediately caught one - `hunt20b`'s 0/1150, S13's own published figure, is refused. Numbers, denominators and fallback rates: `docs/measurements.md` §2026-09-01 (S16). Ten tests, four mutants killed each by its own named test. | judgment | inline | done |
| ~~**S17**~~ | ~~Changeling randomness instrument.~~ **CLOSED 2026-09-01T15:27:47+00:00**, inline rather than by the `codex` the row named. `eval/changeling_claims.py` reads what seats SAY about themselves against what the night dealt and showed them, off S2's 200 games with no GPU. It reuses cabal's claim matcher rather than adding a fourth - `_claim_en` gained an adornment gap ("I'm the REAL Seer") and a sibling `_claim_en_dealt` for the past-tense shape this rung produces and cabal cannot have, since a cabal role never moves. **The widening moves no published figure** (S13 7/1580, S16 0/1580 on `hunt20c`, pinned by a test; a narrowing mutant kills the pin). **The design call the records forced:** scoring a deal claim against `dealt` alone reported the referee's own pre-S14 self-line as lying - 65 of the 74 such claims name the card the night showed later - so both shapes score against `{dealt, belief}` and the split is printed beside the rate. **The finding:** the table is NOT the all-honest collapsed game `docs/open-arms.md` warns about. Village-believing seats name a card they were never shown on 14.0% of deal claims and 15.0% of present claims; wolf-believing seats on 32.9% of present claims. Numbers, denominators, both chance bars and the 42.2% blind spot: `docs/measurements.md` §2026-09-01 (S17); evidence `transcripts/changeling-s17-stated-claims.md`. 24 tests, five guards mutation-checked each by its own named test. | judgment | inline | done |
| ~~**S18**~~ | ~~Waker deck campaign design.~~ **CLOSED 2026-09-02 - criterion and recipe committed, no record launched.** `SETUP_6_WAKER` (6/3, 9 cards) is registered, `--seats` is plumbed through `eval.run_changeling`, and `eval.strata --deck SETUP_6_WAKER` prices it. The bar was MEASURED not asserted: `--arm random --seats 6 --games 4000`, derived per-vote chance **30.14%**, arm blind accuracy 28.82% [27.58%, 30.11%] over 5445 votes; the higher is the bar. Power at 200 games is ~272 blind votes, clearing from a true 36% up - better headroom than S2 bought at five seats, because a wider table lowers the bar faster than it thins the stratum. Two guards mutation-checked, each killed by its own named test; the deck test was VACUOUS on first write (asserted on `SETUPS[6]`, so a mutant that ignored the flag passed) and now asserts on the record. Found on the way: `_chance` hardcoded 5 and 4, so a six-seat run would have been scored against SETUP_5's bar silently, and it died on JSON string keys when a reader recomputed the bar off a written record. `docs/changeling-waker-criterion.md`, `eval/runs/changeling-waker.cmd`. | - | - | done |
| ~~**S19**~~ | ~~Waker deck campaign.~~ **CLOSED 2026-09-02 - GATE #3 HOLDS ON SIX SEATS.** 200/200 games, `PARLOR DONE rc=0 elapsed=20183s` (5.61 h), fallback **0.31% of 3600**, recovered 6.83%, no void. Blind villager accuracy **120/262 = 45.80%**, Wilson floor 39.87% and bootstrap floor 38.87%, both clearing the pre-committed **30.14%** - and the criterion demanded BOTH in advance, which is the clause S5 could only record after the fact. The own-arm clause came within 0.02 points of firing: the paired control read 31.12% against a 1.00% tolerance, and the bar stayed the criterion's. Power reproduced (262 blind votes vs ~272; clears from 36% up, not 35%). Gate #2 read, NO VERDICT, no bar was declared. The waker split reads as nothing and that refutes nothing - the waker is not in the blind stratum. **One interim peek disclosed** and cited nowhere. Verdict `eval/waker_verdict.py` (new module - `s5_verdict` would have applied S2's five-seat bar at exit 0), transcript `transcripts/changeling-waker1.md`, numbers `docs/measurements.md`, read `games/changeling/RULES.md` §Waker-deck read. Four decision points mutation-checked; the both-floors guard survived its first test and needed a clustered record where Wilson clears and the bootstrap does not. | - | - | done |
| ~~**S24**~~ | ~~Off-map faction heartbeat spike.~~ **CLOSED 2026-09-02** - `games/heartbeat/`, one faction with no seat, three action types, a tick schedule that is `random.Random(f"schedule:{seed}")` and nothing else, propagation by place. **The snapshot audit is sound at this size**: each render carries the entitlement snapshot taken in the step it was built; a fixture of six leaks, four of whose facts went public at t+1 by another route, reads 6/6 caught against the snapshot and 4/6 MISSED against entitlement recomputed at end of run. The guard went red against the recompute loop first; two mutants killed by their own named tests. Faction decisions tally in their own denominator with their own fallback rate. It is the SECOND consumer of `games/durf/facts.py` - promotion evidence, promotion left for a later slice; nothing in `core/`. Found on the way: a fact whose statement lacks its own term audits clean, closed here by `World.add_fact` and filed as a durf row. Worker branch merged. | judgment | claude worktree | done |
| ~~**S25**~~ | ~~Group-sequential campaign design.~~ **CLOSED 2026-09-02** - `eval/sequential.py` (stdlib only: normal CDF via `erf`, the boundary recursion on a grid anchored at the cut), classic O'Brien-Fleming and Pocock constants reproduced within 0.002 of the published tables, Lan-DeMets spending for arbitrary look fractions, an undeclared-look guard that went red first, `refit_final` for a final look that does not land on the priced N. Null simulation 20000 trials: Gaussian statistic 2.51% [2.31%, 2.74%] against 2.5%; the binomial worked instance carries the integer boundary's EXACT type-I 0.0296, same family as the single-look Wilson gate's 0.0289. `docs/group-sequential-criterion.md` is the template plus the worked instance for the next changeling paired arm (bar 30.14%, two looks): the two-look OBF design costs 0 extra hits at full information. Open in the doc: whether the bootstrap floor applies at an interim, and the spending-function values are not yet cited to a first-hand table. Worker branch merged. | judgment | claude worktree | done |
| ~~**S26**~~ | ~~Solver-seat control read.~~ **CLOSED 2026-09-02** - the instrument gap is closed: `SolverPolicy` reports which mode it acted in, the record and summary carry `solver_mechanical` / `solver_deferred`, and a deferred draw is counted as neither a fallback nor a model decision (guard red first, mutant killed by its own named test, legacy key sets pinned). Read on 400 games at seeds 20000..20399, backend none, `eval/runs/solver-control.cmd`: **10085/32586 = 30.95% proved**, all from night knowledge and own role, none from mission arithmetic; fallback 0/32586 both arms. Good wins **17.25% [13.86%, 21.26%]** against random's 34.00% on the same seeds, and the reason is the artefact: the solver on EVIL seats votes against its own team, every tainted team is rejected, 0 fail cards over 544 missions. The paired stratum shows random at its 0.7 approve rate on both clean and tainted, so pairing selects nothing. The tell does not reach the hunt; it does reach the outcome column. The arm that stops it is a queue row. `docs/measurements.md`, `transcripts/cabal-solver-control.md`. Worker branch merged. | judgment | claude worktree | done |
| ~~**S29**~~ | ~~Belfry adjudicator retry.~~ **CLOSED 2026-09-01T15:44:11+00:00 on the second leg of its own done-when: a recorded decision that no arm will produce `recovered > 0`.** The CODE landed inline the same day - `ModelAdjudicator` carries the seats' `retries=2` and doubling backoff, `ChoiceEvent.recovered` is set from a RULES refusal that later answered (a transport failure is not recovery, and the three flags partition a call), and the paired verdict PRINTS the recovered rate beside the fallback rate. Both mutation-checked. **The evidence leg is unbuyable and that is the finding.** The twenty calls are twenty setup choices - the diviner's herring registration, taken once per game, live in 20 of the frozen pair's 60 games. v2 fell back 0/20, so no first ask was ever refused and the retry loop never entered a second attempt; re-read on the day at fallback `0/20`, recovered `0/20`, 88.89% unchanged, exit 0. v1 fell back 12/20 and is void at 60%, and re-reads at recovered `0/20` STRUCTURALLY: `recovered` is a play-time field set inside `choose()` from `rule_refusals > 0`, so a re-score cannot recover a fumble that had no second ask to make when it happened. The only record that could carry the field is a fresh arm run at a fallback rate high enough to fumble - void by construction the moment it succeeds. Reasoning, and the two things that would reopen it: `docs/decisions.md` §No arm will produce `recovered > 0`. **S23's entry condition is amended in the same close** to a belfry adjudicator record under the 10% void bar with every scored call the model's, which the v2 pair meets today. | constrained | inline | done |
| ~~**S22**~~ | ~~Changeling discussion-length arm.~~ **CLOSED 2026-09-02T06:38:33Z - recipe and criterion ready to launch, unlaunched.** `docs/changeling-rounds-pair-criterion.md` binds `--rounds 2` vs `--rounds 3` on `folk`, 200 games/arm at seeds 5000..5199 - the same seeds as the name-form pair, so the two-round arm doubles as the same-seeds `folk` record a later vocabulary criterion may cite. Deck, wording and scorer untouched. Recipe `eval/runs/changeling-rounds-pair.cmd` refuses while `cl-skin-pair.log` lacks `PARLOR PAIR DONE` (proved: refused at rc=1 with the card busy). Arithmetic `eval.rounds_pair_verdict` reuses the name-form pair's Newcombe + paired bootstrap and adds a SETTINGS PIN that voids a record whose own `args` disagree with the criterion (smoked: a `greek` control posing as an arm voided on four fields). 5 tests. Written inline, not by the `codex` the row named. | judgment | inline | done |
